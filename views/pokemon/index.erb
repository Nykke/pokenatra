<h2>All Pokemon</h2>
  <% @pokemons.each do |pokemon| %>
    <div class= "pokemon"
    <h2><a href="pokemons/<%= pokemon.id %>"><%= pokemon.name %></a></h2>
        <img src="<%= pokemon.img_url %>">
      </div>
  <% end %>
